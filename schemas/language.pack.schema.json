{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://robert-dean.github.io/deadair-community/schemas/language.pack.schema.json",
    "$comment": "Generated by scripts/language.schema.sync.mjs from the ConsoleLanguagePack contract in the deadair repository. Do not edit by hand.",
    "title": "Console language pack",
    "type": "object",
    "properties": {
        "format": {
            "type": "string",
            "const": "deadair.console-language",
            "description": "Says the file is a console language pack"
        },
        "version": {
            "description": "The version of the document's shape. This station reads version 1",
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
        },
        "locale": {
            "type": "string",
            "minLength": 2,
            "maxLength": 35,
            "description": "The language, as a BCP 47 tag such as `de` or `pt-BR`. Never English, which is built into the console"
        },
        "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "description": "The language's name in itself, as the console's picker shows it: `Deutsch`, not `German`"
        },
        "direction": {
            "type": "string",
            "enum": [
                "ltr",
                "rtl"
            ],
            "description": "Which way its text runs"
        },
        "madeFor": {
            "type": "string",
            "maxLength": 50,
            "description": "The console version the pack was translated against. Empty when the file did not say"
        },
        "catalog": {
            "type": "object",
            "propertyNames": {
                "type": "string"
            },
            "additionalProperties": {},
            "description": "The strings, nested by namespace and then by key, in the English catalog's shape. Every value is text or a further level of nesting"
        }
    },
    "required": [
        "format",
        "version",
        "locale",
        "name",
        "direction",
        "madeFor",
        "catalog"
    ],
    "additionalProperties": false
}
